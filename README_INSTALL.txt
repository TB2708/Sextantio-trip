OUR PLACES PLANNER V3.2

This app is Tony's private planning tool. It contains the hidden itinerary, timings, reservations, maps, routes, checklists and contingency notes.

Do not install it on your wife's phone.

Publish it from a separate GitHub repository, such as:
our-places-planner

Upload all files to the repository root, enable GitHub Pages from main and /(root), then install it on Tony's iPhone using Safari → Share → Add to Home Screen.
